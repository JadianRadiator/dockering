FROM ginuerzh/gost:latest

ENTRYPOINT ["/bin/gost"]

# -L=http://... handles your secure proxy on port 8080
# -L=http://:10000 handles a completely open port just for Render's pings
CMD ["-L=http://JadianRadiator:MaJiCkA@:8080", "-L=http://:10000"]

EXPOSE 8080
EXPOSE 10000
