# Use a modern, multi-protocol proxy engine
FROM ginuerzh/gost:latest

ENTRYPOINT ["/bin/gost"]

# Explicitly declare the scheme (http), credentials, and the bind port
CMD ["-L=http://JadianRadiator:MaJiCkA@:8080"]

EXPOSE 8080
